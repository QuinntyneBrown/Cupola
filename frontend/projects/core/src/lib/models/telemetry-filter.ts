/**
 * B08 — Telemetry filters (contract skeleton, open contract).
 * Owner: C10 · Consumers: C06 (request options), C07, C08 · Stability: medium.
 *
 * Open contract: <TO SUPPLY: telemetry filter definition schema>.
 * Committed as a typed placeholder referenced by TelemetryRequestOptions (B06) so
 * imports compile while the surface is resolved.
 * See docs/capability-contracts/cross-capability-contracts.md.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface TelemetryFilter {}
